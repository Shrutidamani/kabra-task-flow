[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Build the TanStack Start server output for Netlify instead of the default Cloudflare target.
  NITRO_PRESET = "netlify"
  NODE_VERSION = "20"
